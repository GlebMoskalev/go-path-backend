completions:
  - name: strconv
    doc: "Package strconv implements conversions to and from string representations of basic data types"
    symbols:
      - name: ParseFloat
        kind: function
        detail: "func ParseFloat(s string, bitSize int) (float64, error)"
        doc: "ParseFloat converts the string s to a floating-point number with the precision specified by bitSize: 32 for float32, or 64 for float64. Use bitSize=64 for float64."
