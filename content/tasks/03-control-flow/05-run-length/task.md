---
title: "Run-length кодирование"
description: "Сжимает строку методом run-length encoding: 'aaabbc' → '3a2bc'"
order: 5
difficulty: medium
hints:
  - "Итерируйтесь по строке с помощью for range, отслеживайте текущий символ и счётчик его повторений"
  - "Не забудьте записать последнюю серию символов после выхода из цикла"
---

# Run-length кодирование

Напишите функцию `RunLength`, которая принимает строку и возвращает её сжатую версию методом run-length encoding.

Правило: каждая серия одинаковых символов заменяется количеством повторений и символом. Если символ встречается один раз — число 1 не пишется.

## Пример

| Вход        | Выход      |
|-------------|------------|
| `"aaabbc"`  | `"3a2bc"`  |
| `"aabbcc"`  | `"2a2b2c"` |
| `"abc"`     | `"abc"`    |
| `""`        | `""`       |
| `"aaaa"`    | `"4a"`     |

