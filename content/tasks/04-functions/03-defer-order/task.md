---
title: "Порядок defer"
description: "Демонстрирует LIFO-порядок выполнения defer через сбор меток в слайс"
order: 3
difficulty: medium
hints:
  - "Используйте именованный возвращаемый параметр, чтобы defer мог модифицировать результат"
  - "Замыкание в defer захватывает переменную по ссылке — создайте локальную копию внутри цикла, чтобы сохранить значение итерационной переменной"
---

# Порядок defer

Напишите функцию `CollectDeferred`, которая принимает слайс строк `labels []string` и возвращает новый слайс — те же строки, но в **обратном порядке**.

**Ограничение:** для разворота используйте только механизм `defer`. Добавляйте каждый элемент к результату через `defer`-вызов, и они выполнятся в LIFO-порядке.

## Пример

| Вход                   | Выход                  |
|------------------------|------------------------|
| `["a", "b", "c"]`      | `["c", "b", "a"]`      |
| `["first", "second"]`  | `["second", "first"]`  |
| `["only"]`             | `["only"]`             |
| `[]`                   | `[]`                   |
