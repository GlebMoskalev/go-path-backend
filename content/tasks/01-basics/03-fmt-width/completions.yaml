completions:
  - name: fmt
    doc: "Package fmt implements formatted I/O"
    symbols:
      - name: Sprintf
        kind: function
        detail: "func Sprintf(format string, a ...any) string"
        doc: "Sprintf formats according to a format specifier and returns the resulting string. Use %-20s for left-aligned string in 20-char field, %4d for right-aligned integer in 4-char field."
