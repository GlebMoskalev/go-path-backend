---
title: "Стек"
description: "Реализует структуру данных стек с методами Push, Pop и IsEmpty"
order: 2
difficulty: easy
hints:
  - "Методы `Push` и `Pop` изменяют состояние стека — используй **pointer receiver**"
---

# Стек

Реализуйте структуру данных «стек» (LIFO — последним вошёл, первым вышел).

Структура `Stack` уже объявлена в шаблоне. Реализуйте три метода:

- `Push(val int)` — добавляет элемент на вершину стека
- `Pop() (int, bool)` — снимает элемент с вершины и возвращает `(значение, true)`. Если стек пустой — `(0, false)`
- `IsEmpty() bool` — возвращает `true`, если стек пустой

Методы `Push` и `Pop` должны изменять состояние стека.

## Пример

```go
s := &Stack{}
s.Push(1)
s.Push(2)
s.Pop()    // (2, true)
s.Pop()    // (1, true)
s.Pop()    // (0, false)
s.IsEmpty() // true
```
